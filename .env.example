# Server-side only. Never prefix these values with VITE_.
OPENAI_API_KEY=
GEMINI_API_KEY=

# Override these only with vision-capable models available to the account.
OPENAI_READER_MODEL=gpt-5.6
OPENAI_JUDGE_MODEL=gpt-5.6
GEMINI_READER_MODEL=gemini-3.7-flash

PORT=4173
